# Tests Layout

- `tests/smoke.ts`: End-to-end MCP smoke verification.
- `tests/integration/`: Future cross-module behavior tests.
- `tests/unit/`: Future isolated logic tests.

Keep test assets under `tests/` only. Do not add ad-hoc test scripts inside `src/`.
