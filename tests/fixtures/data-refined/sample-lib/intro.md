# Librarian Fixture

## MCP Smoke Fixture

This markdown file is used by `tests/smoke.ts` as a deterministic fallback dataset.

It intentionally includes the keyword MCP so grep-based smoke checks remain stable.

Use this fixture only for test verification, not as production knowledge content.
